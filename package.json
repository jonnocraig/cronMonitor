{
  "name": "cron-monitor",
  "version": "1.0.0",
  "type": "module",
  "description": "Website change monitor with mobile notifications",
  "scripts": {
    "start": "bun run src/index.ts",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "cheerio": "^1.0.0-rc.12"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.3.0",
    "vitest": "^2.0.0"
  }
}
