# Required: URL to monitor for changes
MONITOR_URL=https://example.com/page-to-watch

# Required: ntfy.sh topic for notifications (use a unique, hard-to-guess name)
NTFY_TOPIC=my-unique-topic-name

# Optional: Fetch timeout in milliseconds (default: 30000)
FETCH_TIMEOUT_MS=30000
